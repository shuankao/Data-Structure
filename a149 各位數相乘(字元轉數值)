#include<iostream>

using namespace std;


int main(){
	string a;
	int n;
	cin >> n;
	for (int i = 0; i < n; i++) {
		cin >> a;
		int sum = 1;
		for (int j = 0; j < a.size(); j++) {
			sum *= a[j]-'0';//字元減去字元'0'，可得該字元代表之數值，並存入整數型別的sum
		}
		cout << sum << endl;
	}
	return 0;
}
