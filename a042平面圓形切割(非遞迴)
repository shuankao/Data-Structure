#include<iostream>

using namespace std;

int main()
{
	int a, b[10000];
	b[1] = 2;
	while (cin >> a)
	{
		for (int i = 1; i < a; i++)
		{
			b[i + 1] = b[i] + 2 * i;
		}
		cout << b[a] << endl;
	}
}
